#include<bits/stdc++.h>

using namespace std;

int main(){

  int t;
  cin>>t;
  while (t--){ 
    int n, k;
    cin>>n>>k;
    // int as[n];
    for (int i = 0; i < k; i++){
      int a;
      cin>>a; 
    }
  }

  return 0;
}