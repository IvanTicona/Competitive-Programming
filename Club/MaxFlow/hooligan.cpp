/*
Author: Ivan Ticona
*/
#include<bits/stdc++.h>

using namespace std;

int main(){

  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);

  int N, M, G;

  while (cin >> N >> M >> G && N && M && G)
  {
    for (int i = 0; i < G; i++)
    {
      int a, b;
    }
    
  }
  

  return 0;
}