#include<bits/stdc++.h>

using namespace std;

int main(){

  int n; cin>>n;

  while (n--){
    int m; cin>>m; // number of coins
    int coins[m];
    memset(coins,0,sizeof(coins));
    for (int i = 0; i < m; i++) cin>>coins[i];
    
    

  }
  

  return 0;
}